const styles = {
  // ── Layout ───────────────────────────────────────────────────
  paddingX: "sm:px-16 px-6",
  paddingY: "py-section",
  padding:  "sm:px-16 px-6 py-section",

  // ── Hero ──────────────────────────────────────────────────────
  heroHeadText: "font-display font-bold text-display text-text-primary leading-tight",
  heroSubText:  "font-sans text-body-lg text-text-secondary mt-4 leading-relaxed",

  // ── Section titles ────────────────────────────────────────────
  sectionHeadText: "font-display font-bold text-h1 text-text-primary",
  // eyebrow label above section head — uses mono + accent for the "data" signal
  sectionSubText:  "font-mono text-label text-accent uppercase tracking-widest",

  // ── Metric callouts (used on $41M etc.) ───────────────────────
  metricText: "font-mono font-bold text-metric",
};

export { styles };
